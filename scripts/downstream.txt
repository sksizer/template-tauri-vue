# Projects instantiated from this template.
# One repo URL per line. Lines starting with # are comments.
#
# For private/unpublished projects, create scripts/downstream.local.txt
# (gitignored) using the same format. The _all.sh batch scripts read both
# files and operate on the concatenated list.
